name: Deploy to WordPress.org
on:
  release:
    types:
      - created
      - edited

jobs:
  deploy:
    name: On Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: PHP version
        run: php --version

      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "::set-output name=dir::$(composer config cache-files-dir)"

      - name: Cache Composer vendor directory
        uses: actions/cache@v1
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install PHP Dependencies
        run: composer install --no-progress --no-dev --optimize-autoloader

      - name: Prepare files
        run: rsync -r --exclude-from=.distignore . ${{runner.temp}}/${{ steps.package.outputs.PACKAGE }}

      - name: Create WordPress.org assets directory
        working-directory: ${{runner.temp}}
        run: mkdir .wporg

      - name: Copy WordPress.org assets
        run: rsync -r .wporg/ ${{runner.temp}}/.wporg

      - name: List Files
        working-directory: ${{runner.temp}}
        run: find .

      - name: WordPress Plugin Deploy
        uses: 10up/action-wordpress-plugin-deploy@master
        working-directory: ${{runner.temp}}/${{ steps.package.outputs.PACKAGE }}
        env:
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          ASSETS_DIR: .wporg
